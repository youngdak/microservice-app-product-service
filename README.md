Step to run the application

1. Go to the docker folder
2. Run the script "run-docker-compose.sh"
3. Run the script "run-product-service.sh"
4. Run the script "run-api-gateway.sh"
5. Run the script "run-angular-frontend.sh"
6. The username and password to login to keycloak is 
    username: testuser
    password: password
